<!DOCTYPE html>
<html>

<head>
    <%= render "shared/head" %>
</head>

<body class="bg-gray-100 font-sans">
    <div class="flex min-h-screen">
        <!-- Sidebar -->
        <%= render "shared/private_sidebar" %>

            <!-- Main Content Area -->
            <div class="flex-1 flex flex-col">
                <!-- Navbar -->
                <%= render "shared/private_navbar" %>

                    <!-- Main Content -->
                    <div class="bg-grey-50 p-8">
                        <%= yield %>
                    </div>
            </div>

    </div>
</body>

</html>